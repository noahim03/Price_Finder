import { StrictMode } from "react";
import { LightCover } from "./screens/LightCover";

export const App = (): JSX.Element => {
  return (
    <StrictMode>
      <LightCover />
    </StrictMode>
  );
};

export default App; 